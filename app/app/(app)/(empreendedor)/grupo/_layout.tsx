import { Stack } from "expo-router";

export default function GrupoLayout() {
  return (
    <Stack
      screenOptions={{
        headerStyle: { backgroundColor: "#0A0D14" },
        headerTintColor: "#fff",
      }}
    />
  );
}
