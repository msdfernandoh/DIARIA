/**
 * Config gerado no build da Vercel (scripts/inject-web-config.js).
 * Local: copie config.example.js → config.js e preencha.
 */
window.DIARIA_CONFIG = window.DIARIA_CONFIG || {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
